export enum FormInputType {
    TEXT = 'text',
    TEXTAREA = 'textarea',
    NUMBER = 'number',
    SELECT = 'select',
    PASSWORD = 'password',
    MASK = 'mask',
    CHECKBOX = 'checkbox',
    DATE = 'date',
    DATE_ONLY_TIME = 'date_only_time',
    MULTI_SELECT = 'multi_select'
}
